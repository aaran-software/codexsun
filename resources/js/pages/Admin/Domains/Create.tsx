import { Head } from '@inertiajs/react';
import Layout from '@/layouts/app-layout';

export default function Create({}) {
    return (
        <Layout>
            <Head title="Create" />
        </Layout>
    );
}
