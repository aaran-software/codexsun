PROMPT NAME: 001_INITIAL_INFRASTRUCTURE_SETUP

ROLE

You are a **Senior Software Architect and .NET Platform Engineer** responsible for maintaining the Codexsun platform.

Before performing any action you MUST read:

/ASSIST/AI_RULES.md

Follow every rule defined in that file.

You must also analyze the project before implementing any changes.

---

FILES TO ANALYZE FIRST

Read the following files completely:

/ASSIST/AI_RULES.md
/ASSIST/ARCHITECTURE.md
/ASSIST/STRUCTURE.md
/ASSIST/DATABASE.md
/ASSIST/SECURITY.md

Understand the architecture and repository layout before proceeding.

---

OBJECTIVE

Implement the **initial infrastructure setup** for the Codexsun platform.

This includes:

• Docker infrastructure
• PostgreSQL container
• Redis container
• Aspire orchestration alignment
• Test project for database validation
• Naming corrections
• Consistent port configuration

Do NOT modify unrelated modules.

---

SERVICE OVERVIEW

Aspire → orchestration and service defaults
cxserver → ASP.NET Core backend
cxstore → React frontend

These services must remain unchanged but must integrate with the infrastructure.

---

PORT STANDARDIZATION

All services must use the following ports.

7020 → Aspire host
7021 → cxserver HTTP
7022 → cxserver HTTPS
7023 → cxstore frontend
7024 → Redis
7025 → PostgreSQL

Ensure all services follow this sequence.

---

DATABASE CONTAINER

Database engine:

PostgreSQL

Container configuration:

username: cxadmin
password: DbPass1@@
database: codexsun

Port:

7025

---

REDIS CONTAINER

Redis must run in a container.

Port:

7024

---

CONTAINER INFRASTRUCTURE

Create a new folder in the root of the repository:

.container

Use the following **production-style container structure**.

.container/
│
├── docker-compose.yml
│
├── postgres/
│   ├── Dockerfile
│   └── init/
│       └── init.sql
│
├── redis/
│   └── redis.conf
│
└── env/
└── dev.env

The compose file must start:

• PostgreSQL
• Redis

Volumes must persist data.

---

DOCKER IMAGE

Create Docker build configuration for the platform.

Image name:

Codexion

Version:

v6

Compose must build using:

Codexion:v6

---

TEST PROJECT

Create a new project:

cxtest

Framework:

xUnit

Add the project to:

Codexsun.slnx

Purpose:

Verify database connectivity.

Test class:

DatabaseConnectionTests

Test must:

1. Connect to PostgreSQL
2. Execute query

SELECT 1

3. Assert result success

Test must PASS.

---

DATABASE CONNECTION STRING

Host=localhost;Port=7025;Database=codexsun;Username=cxadmin;Password=DbPass1@@

---

NAMING CONSISTENCY AUDIT

Scan the entire solution and correct naming issues.

Examples:

cxApphost → cx.AppHost
Cxserver → cxserver
Frontend → cxstore

Verify consistency across:

• project names
• namespaces
• docker configuration
• Aspire configuration
• solution references

---

VALIDATION

After implementation verify:

1. Solution builds successfully
2. Docker compose runs containers
3. PostgreSQL starts correctly
4. Redis starts correctly
5. Aspire host runs on port 7020
6. API runs on ports 7021 / 7022
7. Frontend runs on port 7023
8. Database test passes
9. Ports are sequential
10. No naming inconsistencies exist

---

DOCUMENTATION

Update the following documentation files if required:

DATABASE.md
STRUCTURE.md
PROJECT_LOG.md

Add a new entry in the project log.

---

OUTPUT

Provide:

1. Updated repository structure
2. docker-compose.yml
3. Dockerfiles
4. Test project files
5. Build and run instructions
6. Test execution result

Ensure the solution compiles successfully.

Follow Clean Architecture rules.

Never break module boundaries.
