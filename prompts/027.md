PROMPT NAME: 013_DATABASE_STRUCTURE_AUTH_TO_VENDOR

ROLE

You are a **Senior .NET Backend Architect and Database Engineer** responsible for defining the database schema structure for the Codexsun platform.

Before doing anything you MUST read:

/ASSIST/AI_RULES.md

Follow repository rules and architectural standards.

---

OBJECTIVE

Create database table structures for modules from:

Auth → Location → Product → Contacts → Vendor

This prompt defines **table structures only**.

Do NOT implement:

APIs
Services
Repositories
UI

Only database schema and migration.

---

IMPORTANT RULE

Before creating any table:

Check if the table already exists.

If the table exists → SKIP creation.

If the table does not exist → CREATE the table.

This ensures migrations are safe for environments where partial schemas may already exist.

---

DATABASE RULES

Primary key:

Id → integer auto increment

Naming convention:

snake_case

Examples:

product_groups
contact_addresses
vendor_users

---

MODULE ORDER

Tables must be created in dependency order.

---

AUTH DOMAIN

users

Columns:

id
username
email
password_hash
is_active
created_at

---

roles

Columns:

id
name
description

---

permissions

Columns:

id
name
code

---

role_permissions

Columns:

id
role_id
permission_id

Foreign keys:

role_id → roles.id
permission_id → permissions.id

---

user_roles

Columns:

id
user_id
role_id

Foreign keys:

user_id → users.id
role_id → roles.id

---

refresh_tokens

Columns:

id
user_id
token
expires_at
revoked_at
created_at
ip_address

Foreign key:

user_id → users.id

---

LOCATION DOMAIN

countries

Columns:

id
name
country_code
is_active

---

states

Columns:

id
name
state_code
country_id

Foreign key:

country_id → countries.id

---

districts

Columns:

id
name
state_id

Foreign key:

state_id → states.id

---

cities

Columns:

id
name
district_id

Foreign key:

district_id → districts.id

---

pincodes

Columns:

id
code
city_id

Foreign key:

city_id → cities.id

---

PRODUCT MASTER DOMAIN

brands

Columns:

id
name
is_active

---

colours

Columns:

id
name
is_active

---

sizes

Columns:

id
name
is_active

---

units

Columns:

id
name
short_name
is_active

---

hsncodes

Columns:

id
code
description
tax_rate

---

PRODUCT CATALOG STRUCTURE

product_groups

Columns:

id
name
description
is_active

---

product_types

Columns:

id
name
product_group_id

Foreign key:

product_group_id → product_groups.id

---

product_categories

Columns:

id
name
parent_id
product_group_id

Foreign keys:

parent_id → product_categories.id
product_group_id → product_groups.id

---

CONTACT DOMAIN

contacts

Columns:

id
name
email
phone
contact_type_id
is_active

---

contact_addresses

Columns:

id
contact_id
address_line1
address_line2
city_id
district_id
state_id
country_id
pincode_id

Foreign keys:

contact_id → contacts.id

city_id → cities.id
district_id → districts.id
state_id → states.id
country_id → countries.id
pincode_id → pincodes.id

---

VENDOR DOMAIN

companies

Columns:

id
name
registration_number
tax_number
country_id
state_id
city_id
pincode_id

Foreign keys:

country_id → countries.id
state_id → states.id
city_id → cities.id
pincode_id → pincodes.id

---

vendors

Columns:

id
company_id
contact_id
brand_name
is_active

Foreign keys:

company_id → companies.id
contact_id → contacts.id

---

vendor_users

Columns:

id
vendor_id
user_id

Foreign keys:

vendor_id → vendors.id
user_id → users.id

---

vendor_addresses

Columns:

id
vendor_id
address_line1
address_line2
city_id
state_id
country_id
pincode_id

Foreign keys:

vendor_id → vendors.id

city_id → cities.id
state_id → states.id
country_id → countries.id
pincode_id → pincodes.id

---

INDEX RULES

Create indexes for:

users.email
users.username

contacts.email
contacts.phone

cities.name
brands.name

product_groups.name

---

MIGRATION

Create migration:

DatabaseStructureAuthToVendor

Command:

dotnet ef migrations add DatabaseStructureAuthToVendor

Then apply:

dotnet ef database update

---

VALIDATION

Verify:

Tables created successfully.

Foreign keys valid.

No duplicate tables created.

Migration runs successfully on empty database.

Migration safely skips existing tables.

---

OUTPUT

Provide:

1. Final table list
2. Entity models
3. Migration file
4. DbContext updates
5. Schema validation

Ensure solution builds successfully.
