# Cxsun Backend

This folder is the backend and orchestration starting point for `cxsun`.

Current state:

- typed event bus bootstrapped through `@codexsun/core`
- first HTTP host runtime exists
- first health module exists
- plugin routes now mount through the cxsun host runtime
