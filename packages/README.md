# Packages

This folder holds shared packages for the platform.

Current scaffold:

- `core`
- `ui`
- `types`
- `utils`

Current shared UI owner:

- `ui` owns reusable UI components, shared UI helpers, and global frontend style tokens

Current shared core owner:

- `core` owns platform contracts and shell registry helpers that should not live inside a specific app
